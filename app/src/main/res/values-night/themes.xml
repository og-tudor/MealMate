<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MealMate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Make status bar translucent -->
        <item name="android:statusBarColor">@color/light_black</item>
        <item name="android:navigationBarColor">@color/light_black</item>
        <item name="android:windowLightStatusBar">false</item> <!-- Dark text is not used in dark theme -->

    </style>
</resources>